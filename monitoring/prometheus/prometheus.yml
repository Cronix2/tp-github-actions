global:
  scrape_interval: 5s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: flask-app
    metrics_path: /metrics
    static_configs:
      - targets:
          - web-blue:5000
          - web-green:5000
